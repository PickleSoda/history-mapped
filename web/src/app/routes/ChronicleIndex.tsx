/** Chronicle list (placeholder). The catalog of guided tours lands in the
 *  chronicles pass; routing + the persistent shell are wired now. */
export function ChronicleIndex() {
  return (
    <div className="p-4">
      <h2 className="text-sm font-semibold text-neutral-500">Chronicles</h2>
      <p className="mt-3 text-sm text-neutral-400">Tour catalog — coming soon.</p>
    </div>
  );
}
